class ResponseRecomendApp:
    recvQueue = []
    sendQueue = []
    empty_data = False

    def recvQueueEnque():
        '''
        1. recvThread에 들어온 dict메세지를 recvQueue에 집어넣는다.

        '''
        return
    def recvQueueDeque():
        return
    def sendQueueEnque():
        return
    def sendQueueDeque():
        '''
        1. sendQueue가 비어있지 않다면 진행
        2. dict메세지를 반환? 전송?한다.
        '''
        return
    def isEmptySendQueue():
        '''
        1. sendQueue가 비어있는지 확인한다.
        2. 비어있다면 true, 비어있지 않다면 false를 반환한다.
        '''
        return bool

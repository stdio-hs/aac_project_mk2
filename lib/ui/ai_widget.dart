import 'package:flutter/material.dart';

class AiWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI Page')),
      body: Center(
        child: Text('AI Widget'),
      ),
    );
  }
}
